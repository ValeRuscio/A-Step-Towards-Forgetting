# Reuse an existing CUDA-compatible torch>=2.6 installation.
# Do not install a different Torch build into an active experiment environment.
transformers==4.57.6
numpy>=1.26,<3
matplotlib>=3.8,<4
huggingface-hub>=0.34,<1
safetensors>=0.4
# Needed only for the natural-text task:
datasets>=3,<5
